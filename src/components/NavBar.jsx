import NavItem from './NavItem';

const NavBar = () => (
  <nav className="navContainer">
    <div>
      <h1>Bookstore CMS</h1>
      <NavItem />
    </div>
    <p>Profile</p>
  </nav>
);

export default NavBar;
